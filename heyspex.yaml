schema_version: 2
vault_root: specs/
# vault_id is intentionally omitted from the template.
# A unique vault_id is minted at registration time (init.ts mintVaultId());
# do NOT hardcode a vault_id here — never the platform UUID.
